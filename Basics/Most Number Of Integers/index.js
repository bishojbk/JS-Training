function findNumAppears(array) {
  let length = array.length;
  let array1 = array[0];
  for (let i = 0; i < length; i++) {
    if ((array[i] = array1)) {
      let count = 0;
      count = count + 1;
    }
  }

  findNumAppears([1, 2, 3, 2, 2, 8, 1, 9]);
}
